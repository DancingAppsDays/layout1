
import {FormArray} from '@angular/forms';   //Manuel115

export class Userinterface {            //was created as class, but is now interface
    id?: string;
    name?: string;
    password?: string;
    email?: string;
    //fecha_registro: string;    //will be created_at
}
