/*import { LoginObject } from './login-object';

describe('LoginObject', () => {
  it('should create an instance', () => {
    expect(new LoginObject()).toBeTruthy();   //missing a object inside... but who cares bout this bish
  });
});*/
